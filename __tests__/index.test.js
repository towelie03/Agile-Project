import supertest from 'supertest'
import app from '../index.js'

describe('POST /login', () => {
	
	describe('inputed a username and password', () => {
		
	});

	describe('when the username or password is missing', () => {
		
	});

});
